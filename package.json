{
  "name": "tasksaw",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron .",
    "dev": "npm run start",
    "lint": "eslint src/**/*.ts --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^37.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.0.0"
  }
}
